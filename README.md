# 🎓 경기북부 직업교육 학사관리 시스템

엑셀 대신 웹 브라우저로 훈련생·출결·강사·강의실을 한 번에 관리하는 시스템입니다.

---

## ✅ 처음 설치하는 분은 여기서 시작하세요 (3단계)

### 📌 1단계 — Supabase 데이터베이스 만들기

> **Supabase**는 구글처럼 무료로 쓸 수 있는 온라인 데이터 저장소입니다.
> 엑셀 파일 대신 여기에 모든 데이터가 저장됩니다.

1. **[supabase.com](https://supabase.com)** 에 접속해서 회원가입(무료) 후 **새 프로젝트**를 만드세요.
2. 프로젝트가 생성되면 왼쪽 메뉴에서 **"SQL Editor"** 를 클릭하세요.
3. 화면 위쪽 **"+ New query"** 버튼을 클릭하세요.
4. 이 저장소에 있는 **`supabase-setup.sql`** 파일을 열어서 전체 내용을 복사하세요.
5. SQL Editor 입력창에 붙여넣고 **"Run"(또는 Ctrl+Enter)** 을 누르세요.
6. 하단에 **"Success"** 메시지가 나오면 데이터베이스 준비 완료!

> 💡 **이미 예전에 SQL을 실행했는데 저장 오류가 나는 경우:**  
> `supabase-setup.sql`을 다시 한 번 실행하면 됩니다.  
> 파일 끝에 `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` 구문이 있어서  
> **기존 데이터는 유지되면서 누락된 컬럼만 추가**됩니다.

---

### 📌 2단계 — index.html에 내 Supabase 주소 연결하기

> Supabase에서 발급받은 "주소(URL)"와 "비밀번호(Key)"를 `index.html`에 넣어야 합니다.

1. Supabase 프로젝트에서 왼쪽 메뉴 **"Project Settings" → "API"** 로 이동하세요.
2. 아래 두 가지 값을 복사해두세요:
   - **Project URL**: `https://xxxxxxxxxxxx.supabase.co` 같이 생긴 주소
   - **anon public key**: `eyJhbGci...` 로 시작하는 긴 문자열

3. 이 저장소의 **`index.html`** 파일을 메모장(또는 VSCode)으로 열고,
   **33번째 줄** 근처를 찾으세요:

   ```
   // ↓↓↓ 여기에 내 Supabase 정보를 입력하세요 ↓↓↓
   const SB_URL = "https://여기에-Project-URL-입력";
   const SB_KEY = "여기에-anon-public-key-입력";
   // ↑↑↑ 입력 완료 후 저장하세요 ↑↑↑
   ```

4. `"https://여기에-Project-URL-입력"` 부분을 Supabase Project URL로 교체하세요.
5. `"여기에-anon-public-key-입력"` 부분을 anon public key로 교체하세요.
6. 파일을 **저장(Ctrl+S)** 하세요.

---

### 📌 3단계 — 브라우저에서 실행하기

1. `index.html` 파일을 **크롬(Chrome) 브라우저**로 여세요.  
   (파일을 더블클릭하거나, 크롬에 드래그&드롭 하면 됩니다)
2. 로그인 화면이 나타납니다.
3. 아이디: **관리자**, 비밀번호: **admin1234** 로 로그인하세요.
4. 🎉 완료! 모든 탭을 자유롭게 사용하세요.

---

## 📂 파일 구성

| 파일 | 설명 |
|------|------|
| `index.html` | 시스템 전체 (이 파일 하나로 모든 화면이 작동) |
| `supabase-setup.sql` | Supabase 데이터베이스 초기 설정 SQL (처음 한 번만 실행) |

---

## 📋 주요 기능 (탭별 설명)

| 탭 이름 | 기능 |
|---------|------|
| 🏠 대시보드 | 전체 현황 요약 |
| 📚 과정 현황 | 훈련 과정 등록·수정·삭제, PDF 강의계획서 첨부 |
| 👥 훈련생 관리 | 훈련생 등록·수정·삭제, 엑셀 내보내기 |
| 🏅 강사 관리 | 강사 정보 등록 |
| 📅 강의실 관리 | 강의실 배정 및 일정 관리 |
| ✅ 출결 관리 | QR코드 스캔 출석 또는 수동 입력 |
| 🎓 수료 관리 | 출석률 기준 수료/미수료 자동 판정 |
| 📄 증명서 발급 | 수료증·수강증명서 인쇄 |

---

## ✅ 출결 관리 — 작동 원리

### 📱 QR 코드 출석 방식

> 강의실 앞 모니터에 QR을 띄워두면, 훈련생이 스마트폰으로 찍어 직접 출석 체크를 합니다.

#### 흐름 요약

```
관리자 PC                훈련생 스마트폰
──────────              ────────────────
① 과정·날짜 선택          ③ QR 코드 스캔
② QR 코드 생성·표시  →   ④ 이름 + 핸드폰번호 입력
                         ⑤ 출석/지각 자동 판별
                         ⑥ "출석 완료" 화면 표시
                     ←   ⑦ 관리자 화면 실시간 반영
```

#### 단계별 설명

| 단계 | 내용 |
|------|------|
| ① 과정·날짜 선택 | "출결 관리" 탭 → **QR 모드** 선택 → 과정 및 날짜 클릭 |
| ② QR 표시 | **"입실 QR"** 또는 **"퇴실 QR"** 을 선택하면 화면에 QR 코드가 나타납니다 |
| ③ QR 스캔 | 훈련생이 스마트폰 카메라로 QR을 스캔 → 모바일 출석 화면이 열립니다 |
| ④ 본인 확인 | 이름과 핸드폰 뒷자리를 입력해 본인을 확인합니다 |
| ⑤ 상태 자동 판별 | 입실 QR 스캔 시: 과정 시작 시각 이전 스캔 → **출석(O)**, 이후 스캔 → **지각(L)** 으로 자동 기록 |
| ⑥ 결과 표시 | 훈련생 화면에 "출석 완료" 또는 "이미 출석함"(중복 방지) 메시지가 표시됩니다 |
| ⑦ 실시간 반영 | 관리자 화면의 훈련생 목록이 실시간으로 업데이트됩니다 |

#### QR 코드 URL 구조

QR에 담기는 URL 형식은 아래와 같습니다:

```
index.html?mode=checkin&cid=<과정ID>&date=<날짜>&type=<in|out>&t=<보안토큰>
```

- `cid` — 과정 고유 번호 (정수)
- `date` — 출결 날짜 (`YYYY-MM-DD`)
- `type` — `in`(입실) 또는 `out`(퇴실)
- `t` — 위·변조 방지용 토큰 (`과정코드-날짜-타입-고정값` 형식)

> ⚠️ **주의**: QR 코드는 데스크탑 브라우저에서 카메라로 스캔할 수 없습니다.  
> 반드시 **스마트폰 카메라 앱**으로 스캔하세요.

---

### 🖊️ 수동 입력 방식

> QR 스캔이 어려운 상황(현장 접속 불가, 사후 정정 등)에서 관리자가 직접 출결을 입력합니다.

#### 흐름 요약

```
① "수동 입력" 모드 선택
② 훈련생 목록 표시 (이미 QR로 기록된 값이 자동으로 채워집니다)
③ 입실 시간 / 퇴실 시간 / 출결 상태 수정
④ 행별 [저장] 버튼 또는 상단 [전체 저장] 버튼 클릭
```

#### 출결 상태 코드

| 코드 | 표시 | 의미 |
|------|------|------|
| `O` | 출석 | 정시 출석 |
| `L` | 지각 | 지각 출석 |
| `A` | 결석 | 결석 |
| `U` | 미확인 | 아직 미처리 |

#### 입력 규칙

- 입실 시간이 비어 있고 상태도 없으면 저장이 차단됩니다.
- **결석(A)** 으로 저장하면 입실 시각은 기록되지 않습니다(phantom check-in 방지).
- 입실 시간만 입력하면 상태가 **자동 판별** 됩니다 — 지각 기준 시각 이전이면 출석(O), 이후이면 지각(L).
- 과정 정보가 불완전하면 저장이 차단되고 "과정 정보 오류" 메시지가 표시됩니다.

#### QR 모드와의 연동

- 수동 입력 화면을 열면 당일 QR 체크인 기록이 자동으로 불러와집니다.
- 수동으로 저장한 값은 QR 모드 화면에도 즉시 반영됩니다.
- 두 방식 모두 `attendance` 테이블의 동일한 행(`student_id + date + course_id` 조합)을 공유하며, 나중에 저장한 값이 덮어씁니다(UPSERT).

---

## ❓ 자주 묻는 질문

**Q. "DB 연결 오류"가 뜹니다.**  
A. `index.html` 33~34번째 줄의 SB_URL, SB_KEY가 올바른지 확인하세요.  
   그리고 `supabase-setup.sql`을 Supabase에서 실행했는지 확인하세요.

**Q. "Could not find the '...' column" 저장 오류가 납니다.**  
A. 이전 버전 SQL로 만든 DB에 새 컬럼이 없거나, Supabase 스키마 캐시가 갱신되지 않은 경우입니다.  
   다음 순서대로 시도해보세요:

1. `supabase-setup.sql`을 **다시 한 번 실행**하세요 — 기존 데이터는 유지되면서 누락된 컬럼이 추가됩니다.  
2. 그래도 같은 오류가 나면: Supabase 대시보드 → **Settings(설정) → API** 메뉴로 이동하여 **"Reload schema"** 버튼을 클릭하세요.  
   (Supabase가 이전 테이블 구조를 기억하고 있을 때 발생합니다)

**Q. 로그인이 안 됩니다.**  
A. `supabase-setup.sql` 실행 후 accounts 테이블에 관리자 계정이 생성됩니다.  
   아이디 `관리자`, 비밀번호 `admin1234`로 로그인하세요.

**Q. 출결 QR코드가 작동 안 합니다.**  
A. 모바일에서 QR을 스캔하면 됩니다. 데스크탑 브라우저에서 직접 스캔은 불가합니다.

**Q. Supabase는 유료인가요?**  
A. 월 5만 행 이하는 완전 무료입니다. 일반 행정 업무에는 충분합니다.
